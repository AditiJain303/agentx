PORT=4000
NODE_ENV=development
DATABASE_URL="file:./dev.db"
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# LLM — at least one required for best results; keyword fallbacks exist
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-haiku-20241022

# Optional market data (Alpha Vantage). If empty, investments use labeled mock data.
ALPHA_VANTAGE_API_KEY=
